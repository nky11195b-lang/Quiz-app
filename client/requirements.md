## Packages
canvas-confetti | Confetti celebration effect for quiz completion
@types/canvas-confetti | TypeScript definitions for canvas-confetti
framer-motion | Smooth page transitions and micro-interactions
date-fns | Human-readable date formatting for leaderboard

## Notes
- We use Google Fonts: "Outfit" for display, "Plus Jakarta Sans" for body. CSS handles the imports.
- Custom Tailwind colors configured via CSS variables to achieve a "Clean Minimal" aesthetic with a sharp blue accent.
